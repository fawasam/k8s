apiVersion: v1
kind: ConfigMap
metadata:
  name: node-app-config
  namespace: default
data:
  # Non-sensitive config — plain text, safe to commit to git
  APP_ENV: "production"
  DB_HOST: "postgres.internal.svc.cluster.local"
  DB_PORT: "5432"
  LOG_LEVEL: "info"
